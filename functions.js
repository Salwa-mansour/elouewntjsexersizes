const less=function(x,y){
    if(x<y)
    console.log(x);
    else if (x == y)
    console.log("Equals")
    else
    console.log(y)
}
//  less(12,24);
 const lessthan=function(x,y){
     if(x<y)
     return x;
     else
     return y;
 }
console.log(lessthan(6,4));